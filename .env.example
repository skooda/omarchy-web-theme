# AMO credentials for sign-firefox.sh. Copy to .env (gitignored) and fill in.
# Generate at https://addons.mozilla.org/en-US/developers/addon/api/key/
# while logged in to the AMO account that owns the add-on.
WEB_EXT_API_KEY=
WEB_EXT_API_SECRET=
